-KLINE library V0.3 includes a few new security algorithms, and improved methods to save some flash/ram usage.

-EcuTool library adds some features for ease of using the ECU tool hardware

-The LiquidCrystal_I2C fixed library includes a fix that affects 20x4 screens 
that causes them to skip a line when you print characters. With it, you can use lcd.print normally.